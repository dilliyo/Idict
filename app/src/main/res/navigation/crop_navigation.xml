<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/btn_confirm">

    <fragment
        android:id="@+id/btn_confirm"
        android:name="com.idic.ui.confirm.ConfirmFragment"
        android:label="@string/title_confirm"
        tools:layout="@layout/fragment_confirm" />

    <fragment
        android:id="@+id/btn_cancel"
        android:name="com.idic.ui.cancel.CancelFragment"
        android:label="@string/title_cancel"
        tools:layout="@layout/fragment_cancel" />

</navigation>